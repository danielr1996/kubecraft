apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: BootstrapProvider
metadata:
  name: talos
  namespace: cabpt-system
spec:
  version: {{ .Values.clusterapi.controlplane.talos.version | quote }}
  configSecret:
    name: gh-token