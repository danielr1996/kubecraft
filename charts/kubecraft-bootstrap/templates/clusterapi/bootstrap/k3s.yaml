apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: BootstrapProvider
metadata:
  name: k3s
  namespace: capi-k3s-bootstrap-system
spec:
  configSecret:
    name: gh-token
  fetchConfig:
    url: "https://github.com/cluster-api-provider-k3s/cluster-api-k3s/releases/{{ .Values.clusterapi.bootstrap.k3s.version }}/bootstrap-components.yaml"