apiVersion: operator.cluster.x-k8s.io/v1alpha2
kind: CoreProvider
metadata:
  name: cluster-api
  namespace: capi-system
spec:
  version: {{ .Values.clusterapi.core.version | quote }}
  configSecret:
    name: gh-token